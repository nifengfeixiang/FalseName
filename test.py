from scipy.stats import uniform
import numpy as np
import copy
from random import sample
from scipy.stats import uniform
from random import sample
# N=uniform.rvs(1,100,1)[0]
# a=np.array([1,2,3])
# b=np.array([2,2,2])
# a={}
# a[1]="ssss"
# a[2]="we"

a=[i for i in range(5)]
print(a)